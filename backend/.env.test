# Application
NODE_ENV=test
PORT=3001
API_PREFIX=api/v1

# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=waste_user
DATABASE_PASSWORD=waste_dev_pass
DATABASE_NAME=waste_management_test

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=test-secret-for-testing-only
JWT_ACCESS_EXPIRATION=3600
JWT_REFRESH_SECRET=test-refresh-secret-for-testing-only
JWT_REFRESH_EXPIRATION=604800

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Throttle
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# IMGBB (free image hosting)
IMGBB_API_KEY=test-api-key
